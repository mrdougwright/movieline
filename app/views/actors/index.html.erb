<h2>Actor's Searched</h2>

<table>
  <tbody class='actor-index'>
    <th>Name</th>
    <th>Popularity</th>
    <% @actors.each do |actor| %>
      <tr>
        <td><%= link_to actor.name, actor_path(actor) %></td>
        <td><%= actor.popularity %></td>
      </tr>
    <% end %>
  <tbody>
</table>